# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo

# Secrets — NEVER commit. Open source repo.
.env
.env.*
!.env.example
*.pem
*.key
secrets/
.supabase/.temp/

# Local vault (personal brain content lives outside the repo).
# Root-anchored so it doesn't also swallow the `src/core/vault/` SOURCE dir.
/vault/
.vault/
/versions/
server/versions/

# oms init secrets (config.yml is safe to commit)
.oms/secrets.env
.oms/*.local

# OS / editor
.DS_Store
*.log
.idea/
.vscode/*
!.vscode/extensions.json

# Supabase CLI local artifacts
supabase/.branches/
supabase/.temp/
